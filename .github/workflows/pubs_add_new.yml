# Run for pull requests (until fixed)
on:
  push:
  pull_request:

jobs:
  call-workflow-start:
    uses: ./.github/workflows/common_start.yml
  call-workflow-end:
    needs:
      - call-workflow-start
    uses: ./.github/workflows/common_end.yml
